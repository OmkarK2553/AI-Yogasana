curr_landmarks_pose0 = results_angle_2_1_pose0.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose0):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose0["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose1 = results_angle_2_1_pose1.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose1):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose1["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose2 = results_angle_2_1_pose2.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose2):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose2["Pose2"]["Angle"].append(curr_dict)



curr_landmarks_pose0 = results_angle_2_2_pose0.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose0):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose0["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose1 = results_angle_2_2_pose1.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose1):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose1["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose2 = results_angle_2_2_pose2.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose2):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose2["Pose2"]["Angle"].append(curr_dict)



curr_landmarks_pose0 = results_angle_2_3_pose0.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose0):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose0["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose1 = results_angle_2_3_pose1.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose1):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose1["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose2 = results_angle_2_3_pose2.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose2):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose2["Pose2"]["Angle"].append(curr_dict)



curr_landmarks_pose0 = results_angle_2_4_pose0.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose0):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose0["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose1 = results_angle_2_4_pose1.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose1):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose1["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose2 = results_angle_2_4_pose2.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose2):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose2["Pose2"]["Angle"].append(curr_dict)



curr_landmarks_pose0 = results_angle_2_5_pose0.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose0):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose0["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose1 = results_angle_2_5_pose1.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose1):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose1["Pose2"]["Angle"].append(curr_dict)

curr_landmarks_pose2 = results_angle_2_5_pose2.pose_landmarks.landmark
curr_dict = {}
for idx, landmark in enumerate(curr_landmarks_pose2):
    curr_dict[mp_pose.PoseLandmark(idx).name] = {        
        'x': landmark.x,
        'y': landmark.y,
        'z': landmark.z
    }
landmarks_pose2["Pose2"]["Angle"].append(curr_dict)
